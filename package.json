{
    "name": "gatekeeper-root",
    "version": "2026.1.0",
    "description": "Root management for UniVerse GateKeeper",
    "scripts": {
        "dev": "npx concurrently \"npm run dev --prefix server\" \"npx wait-on tcp:5000 && npm run dev --prefix client\"",
        "build": "npm install && npm install --prefix server && npm install --prefix client --legacy-peer-deps && npm run build --prefix client",
        "start": "npm start --prefix server",
        "install-all": "npm install --prefix server && npm install --prefix client --legacy-peer-deps",
        "start-server": "npm start --prefix server",
        "start-client": "npm run dev --prefix client"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}